name: validate
on: [pull_request]
jobs:
  binding:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test -d config/sop-bindings && test -f .multiplai/core.lock.yaml
