---
name: content-produce
description: 'End-to-end content production pipeline: takes a content item from brief through publish-ready (or runs any individual stage), with human checkpoints between stages.'
---

# Generated adapter: content-produce

Read and follow the canonical source: `sops/content-produce/SKILL.md` from the Core repository root. Resolve its package references relative to that canonical directory.

This pointer requires the complete Core checkout. Do not install it as a standalone skill or edit it; regenerate with `python tools/generate_adapters.py .`.
