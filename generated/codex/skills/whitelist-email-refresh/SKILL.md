---
name: whitelist-email-refresh
description: Refresh the weekly performance tracker for entity's whitelist email program (BULK + BET). Updates the tracker in place — never regenerates from scratch.
---

# Generated adapter: whitelist-email-refresh

Read and follow the canonical source: `sops/whitelist-email-refresh/SKILL.md` from the Core repository root. Resolve its package references relative to that canonical directory.

This pointer requires the complete Core checkout. Do not install it as a standalone skill or edit it; regenerate with `python tools/generate_adapters.py .`.
