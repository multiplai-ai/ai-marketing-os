# Scene Implementation

Start from the current consumer's approved components and the chosen scene template.
Each scene needs composition size, duration, copy, approved asset references, timing and
export mode. Implement a single example, inspect it in context and then reuse the
structure. Core intentionally ships no styled copy-paste scene with a preselected
entity identity. Historical implementations are retained only in Git history.
